[project]
name = "ingestor"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "feedparser",
    "python-dateutil",
    "sqlalchemy",
    "psycopg2-binary",
    "alembic",
]

[tool.black]
line-length = 88

[tool.ruff]
line-length = 88

